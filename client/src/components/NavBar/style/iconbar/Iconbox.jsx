import { styled } from "@mui/material";

export const Iconbox = styled('div')(({ theme }) => ({
    paddingLeft: '10em',
    paddingRight: '2em',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
    
}));